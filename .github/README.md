dotfiles
--------

# Requirements:

- neovim
- zsh
- brew (mac only)
- yadm
- ctags

# Installation

```bash
yadm clone <url-here>
yadm bootstrap
```

Bootstrap will:

- Install antigen at `~/.antigen/antigen.zsh`
- Install NvChad at `~/.config/nvim`

